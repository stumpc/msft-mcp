changes:
  - section: "Bugs Fixed"
    description: "Extended elicitation (user consent) gate to cover destructive operations (Destructive = true), not just secret operations. Tools that delete, modify, or create Azure resources now prompt the user for confirmation before executing. Consolidated secret and destructive elicitation into a single prompt to avoid double-prompting when both flags are set."